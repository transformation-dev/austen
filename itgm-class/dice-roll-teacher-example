<html>
   <head>
     
 
   </head>
    <body>
 
       <h1>Dice roll game</h1>
       <p id="scores">No scores yet</p>
       <div id="mydice">No dice rolls yet</div>
       <input id="rollButton" type="button" value="ROLL" onClick="countTurn()" />
     
 
   </body>
 
 
</html>
<script>
   let score1 = 0;
   let score2 = 0;
   let die1 = 0;
   let die2 = 0;
 
   function rollDice() {
        die1 = Math.ceil(Math.random() * 6);
        die2 = Math.ceil(Math.random() * 6);
        let myhtml = "Dice 1: " + die1 + " Dice 2: " + die2;
       document.getElementById("mydice").innerHTML = myhtml;
     
      /*
       if ((die1 === 1 && die2 === 1) || score1 >= 50) {
           document.getElementById("scores").innerHTML = "Game over. Score: " + score1;
           document.getElementById("rollButton").style.visibility="hidden";
       } else {
   
        var myHTMLOutput = "Score: " + score1;  
        document.getElementById("scores").innerHTML = myHTMLOutput;
       }
         */
   }
 
 
 
 
   function countTurn() {
       //loop through 20 turns
       for(let turnCounter = 0; turnCounter <= 20; turnCounter++) {
       
        console.log("my turnCounter = " + turnCounter % 2);
        /*  
        if (turnCounter % 2 == 0) {      
           document.write("turn 1 <br />");
           rollDice();  
           score1 += die1 + die2;
       } else {
           document.write("turn 2 <br />");
           rollDice();
           score2 += die1 + die2;
       }
       */
   }
}
     
</script>
 
 
 

